app.controller("lobbiesController", function ($scope) {
    /*$scope.lobbyNo = -1;
    $scope.ws = new WebSocket(serverUrl + '\Lobby');
    $scope.init = new function(){
        ws.onmessage(evt){
            var response = JSON.parse(evt.data);
            if ("newLobby" = response.action)
                lobbyNo = response.lobbyNo;
        }
    }

    $scope.createNewLobby = function(){
        var data = {
            action : 'newLobby'
        }
        ws.send(JSON.stringify(data));
    }
*/
});